fn add(a, b) {
    print a + b;
}

add(1, 2);
add("First ", "Last");

fn print_only_even(n) {
    if (n % 2 == 0) {
        print n;
    }
}

let i = 0;
while (i < 11) {
    print_only_odd(i);
    i = i + 1;
}
