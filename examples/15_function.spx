fn add(a, b) {
    print a + b;
}

print "adding numbers and strings";
add(1, 2);
add("First ", "Last");

fn print_only_even(n) {
    if (n % 2 == 0) {
        print n;
    }
}

print "printing even numbers";
let i = 0;
while (i < 11) {
    print_only_even(i);
    i = i + 1;
}

print "counting";
fn count(n) {
    if (n < 3) {
        count(n + 1);
    }
    print n;
}


count(0);